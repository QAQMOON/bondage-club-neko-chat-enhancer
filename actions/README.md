# 动作库说明

`catgirl-actions.json` 是猫娘插件的远程动作库。

每个动作主题包含：

- `id`：稳定英文标识，建议只用小写字母、数字、短横线。
- `label`：显示在动作轮盘上的按钮文字。
- `enabled`：是否启用。
- `self`：对自己释放时随机抽取的文案。
- `target`：对目标释放时随机抽取的文案，可使用 `{target}` 作为目标名字占位符。

新增主题时向 `actions` 数组追加对象即可。插件会在启动时拉取 GitHub 上的最新动作库，失败时回退到缓存或内置动作。
