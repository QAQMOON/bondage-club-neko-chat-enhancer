# 颜文字库说明

`cute-kaomoji.json` 是猫娘插件的远程颜文字库。

每个分类包含：

- `id`：稳定英文标识。
- `label`：分类说明。
- `enabled`：是否启用。
- `items`：该分类下的颜文字列表。

新增颜文字时，直接向某个分类的 `items` 数组追加字符串即可。新增分类时，向 `groups` 数组追加对象即可。插件启动时会拉取 GitHub 上的最新颜文字库，失败时回退到缓存或内置颜文字。
